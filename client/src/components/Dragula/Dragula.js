// import React from "react";
// import "./Dragula.css";
// import dragula from dragula
// import atoa from atoa

// export const Dragula = props => (

//       dragula.apply(this, atoa(arguments)).on('cloned', cloned)
    
//       function cloned (clone) {
//         rm(clone);
//         atoa(clone.getElementsByTagName('*')).forEach(rm);
//       }
    
//       function rm (el) {
//         el.removeAttribute('data-reactid');
//       }
// )

// export default Dragula;
